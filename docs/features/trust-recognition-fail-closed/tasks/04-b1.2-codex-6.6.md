# Task 04:B1.2 極小 forward-fix(Codex 6.6 完成線+6.7 順序)

> 時點:首輪 cron 運行驗證通過後才施工(6.7 §五.4);v812 凍結期間不動 code。
> 已先行完成(docs,cron 前必須):oracle 修正+狀態措辭+audit SELECT 補記(prod-baseline-20260712.md)。

## P0:safe-deploy 危險 rollback 指引

- [ ] scripts/safe-deploy.mjs:249、:272 移除「flyctl releases rollback」建議,改「部署已發生但驗證未通過;禁止退回 v811(會復活自動認列);保留財務停止線,立即 forward-fix」。
- [ ] verification 紅時輸出可機械辨認的 DEPLOYED_UNVERIFIED,不得只印 deploy complete。
- [ ] safe-deploy.test.mjs:240-263 反向釘死:紅燈輸出不得含 rollback 指令,必須含 forward-fix 與「不得回退 v811」。
- [ ] preflight 新增一閘:本 release 相關外部審查(索引狀態待傳/待裁定/退回)未結,不得進 token 閘(6.7 §六流程教訓)。

## P1:active TrustCard 假已出發

- [ ] FinanceCockpit/TrustCard.tsx:48-49,114-120,193-204 + zh-TW.ts:8885,8894-8895 + en 對應:「已出發待認列/已出發 {date}/出發後才認列轉 Operating」改「舊規則審查日已到/待人工審查」,不得宣稱已出發、可認列、可提領。只改文字與分類名,不重建資料模型。

## P1:證據缺口

- [ ] worker 整合測試移除 gate mock:直接斷言產線 isTrustTransferWriteApproved()===false,餵可成功配對的認列列+帳戶+轉帳,證明 pairsFound=1 但 UPDATE=0(真閘真漏斗)。
- [ ] 守門納入 drizzle/(144 個 migration SQL),SQL 註解略過或歷史檔精確 allowlist;補反引號 `recognizedAt` = NOW() 紅測。
- [ ] processor 層 !db 測試:service throw 後 job reject → failed 告警路徑有測試(不用文字取代)。

## P2:誠實修繕

- [ ] 七處 B1/B1.1 產線註解 2026-07-13 誤標改 2026-07-12(LA 實際施工日)。
- [ ] tasks/03 勾選狀態與 progress 同步(已在 docs 批處理,executor 複核)。

## 完成線

- [ ] tsc 0 錯 + trust 全套綠 + i18n parity 綠 + pnpm ship:test(dry-run)綠
- [ ] fresh 對抗審查
- [ ] 單獨 commit、push、clean tree,才重新交 Jeff ship
