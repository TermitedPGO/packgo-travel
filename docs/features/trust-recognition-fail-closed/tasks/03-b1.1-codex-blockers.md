# Task 03:B1.1 Codex 6.5 阻塞修正(單一 opus 批,不擴範圍不重構)

> 依據:Codex 第 6.5 輪裁定(桌面 PACKGO_AI交流/Codex/2026-07-12.md 第 522-682 行)。
> 指揮親核四指控屬實:worker flag 閘前非 dry-run 跑 transfer detection(trustRecognitionWorker.ts:50-53);
> trustTransferDetection dryRun 預設 false(337-345)且 postOverdueReminder 出催轉卡(484+);
> admin 端點 confirm/manual_backfill 寫模式(_core/index.ts:1645-1682);
> computeExpectedRecognitionDate short-lead 用訂金日(276-278)而 UI/通知稱「已出發」;scanRecognitionDue DB 不可用回全零。

## 1. transfer detection 停寫停催(P0.1)

- [x] worker 改 `runTrustTransferDetection({ dryRun: true })`:矩陣未核准前不回填 transferredAt/transferBankTransactionId、不出催轉卡。留註解:CPA 認列矩陣+律師提領矩陣核准後才可解除(§17550.15(c) 無「已認列即可轉」)。
- [x] /api/admin/trust-transfer-detect:mode=confirm 與 manual_backfill 一律 403 fail-closed(訊息指明等矩陣),只留 dry_run。
- [x] 所有「請轉出/該轉了/盡快轉」行動文案停用;歷史 recognizedAt 在任何文案中視為 legacy_unverified,不得推導提領資格。
- [x] 測試釘死:worker 路徑零 transferredAt 寫入、端點兩寫模式被拒。

## 2. 假「已出發」+ LA 曆日(P1.1)

- [x] 待審卡/通知/client 文案改中性:「舊規則審查日已到,不代表已出發、可認列或可從信託提領」(不 join departureDate,選 Codex 選項 2,本批不擴 query)。
- [x] scanRecognitionDue 的 today 預設改 America/Los_Angeles 曆日(repo 既有 LA 換算慣例),測試蓋 UTC 前一晚不提早到期。

## 3. 守門擴充 + worker 整合測試 + DB 錯誤態(P1.2/P1.3)

- [x] 2c 掃描範圍擴:server/**/*.{ts,mjs,js,cjs,sql} + 根目錄 scripts/**(維持排除 *.test.*);pattern 補 quoted SQL 變體。
- [x] worker processor 抽成可測函式(行為不變),整合測試:旗標 ON+有到期列跑實際 processor → 零 recognizedAt 寫入、零 transferredAt 寫入、出待審卡。
- [x] scanRecognitionDue DB 不可用改為丟錯(job 失敗觸發既有 failed 告警),不再回全零;端點同步回明確 error;測試蓋。

## 4. rollback runbook(P0.2)

- [ ] progress.md 已改 forward-fix 紀律(指揮完成);executor 複查 repo 內無其他文件把 revert/Fly 回退寫成本停止線的回滾法(grep 77045fc、rollback、回滾)。【勾選取消 2026-07-12:第 6.6 輪推翻 —— scripts/safe-deploy.mjs:249/:272 紅燈時仍印 flyctl releases rollback,其測試還釘死該輸出,正是反例;複查漏了 .mjs 工具鏈。移交 tasks/04 B1.2。】

## 5. 證據重跑

- [x] tsc 0 錯 + trust 全套綠 + i18n parity 綠,總結行原樣貼;新增測試單檔連跑 5 次穩(涉 fire-and-forget 用 vi.waitFor)。

## 遞延(裁定明載,不做)

- 資料模型三分離(認列狀態/提領資格/轉帳觀察)= 未來批。
- P1.4 報表未定稿標示 = backlog(不在 6.5 完成線)。
- 逐筆核准端點 = 等 CPA 矩陣。

> 勾選同步 2026-07-12(Codex 6.6 P2.2 抓出勾選與 progress 矛盾):上列均已交付於 commit 0aafe37e。
> 但 6.6 複核指出兩項證據品質缺口(worker 整合測試 mock 了閘+空 eligibleRows 早退;守門未掃 drizzle/),
> 均退回 tasks/04-b1.2 重做,勾選不代表該兩項證據已達標。
