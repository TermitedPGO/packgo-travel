# 記帳 Agent 真實交易實測單 — 2026-05-28

> 拿 prod 上 **Jeff 親手分類過的真實交易**(`jeffOverrideCategory`)當標準答案,
> 跑 M2 的 `preClassify()`,看 agent 答對幾題、哪幾筆規則判錯、哪幾筆接不住。
> **READ-ONLY**:只 SELECT,沒改任何一筆帳、沒改任何規則。

## 怎麼跑(可重跑)
- Harness:`server/services/accountingEvalService.ts`(純函式)+ `server/scripts/accounting-eval.ts`(只讀腳本)+ `accountingEvalService.test.ts`(11/11 綠)。
- 本次:本機跑腳本,`DATABASE_URL` 指向 prod TiDB(只讀)。重跑加年份:`... accounting-eval.ts 2025`。
- 標準答案只認 `jeffOverrideCategory`。沒被 Jeff 改過的交易**不進考題**(沒有可信答案 → 不評分,不腦補)。

## 總分(考題 603 筆)
| 指標 | 數字 |
|------|------|
| 確定性規則命中(conf≥90,省 LLM) | 82(覆蓋 13.6%) |
| 確定性規則精度 | **64.6%**(對 53 / 命中 82) |
| 🔴 規則判錯(硬規則拍板但錯) | **29** |
| 🟠 規則接不住、LLM 當初也分錯 | **297** |
| 🟠 中信心 memo 提示方向錯 | 1 |
| 🟡 規則沉默但 LLM 當初答對 | 37 |
| ⚪ 規則沉默且無 agent 紀錄 | 173 |
| **該動手修的錯誤合計** | **326** |

重點:**29 筆 rule_wrong 全部出自同一條規則 — 業主身分規則。** 其餘 297 是規則涵蓋不到、LLM 當初多半丟 `other_review`,Jeff 再手動分類。

---

## 🔴 Bug 1 — 業主規則「進出不分」,把 29 筆真實營收誤判成 transfer
`accountingKnowledge.ts:193` 註解寫「業主本人 — 進出皆 transfer,覆蓋一切」。實測證明這條太貪心:

| 方向 | 樣本 | 規則判 | Jeff 答案 | 對不對 |
|------|------|--------|-----------|--------|
| 出帳 `Zelle payment **to** CHUN FU HSI` $3,000 | transfer(owner) 95 | transfer | ✅ 對 |
| 進帳 `Zelle payment **from** CHUN FU H` -$1,300 | transfer(owner) 95 | **income_booking** | 🔴 錯 |

進帳側 **29 筆**(`from CHUN FU HSIEH` 系列,2025-05 ~ 2026-05),Jeff 全標 `income_booking`(1 筆 refund)。規則卻全部拍板 transfer conf95 → **直接從營收裡抹掉這些錢**,會嚴重低報公司收入。

- 出帳側(Jeff 拿錢出去給自己)判 transfer 是**對的**,Jeff 認同。
- 問題只在**進帳**:對方欄位掛著「Chun Fu Hsieh」的進帳,Jeff 當營收,不是內部轉帳。
- 需要 Jeff 確認原因後才改(見文末提問)。

## 🟠 Bug 2 — Ann 供應商規則查錯欄位,24 筆 cogs_tour 全漏接
`accountingKnowledge.ts:220` Ann 用 `counterparty-word` 模式(只比對「對方欄位」,避免 `annual`/`channel`/`Ann Arbor` 誤命中)。但實際交易的對方名只出現在**描述**裡(`Zelle payment to Ann for "Ch..."`),對方欄位是 null → 規則永遠不命中。

- `Zelle payment to Ann ...` 共 **24 筆**,Jeff 全標 `cogs_tour`(簽證代辦),規則全漏。
- 對照:`Zelle payment to JUPITER LEGEND` 用 `contains` 模式比對整段描述 → **每筆都命中** ✅(rule_correct 抽樣裡 9/10 是 Jupiter)。
- 修法:讓 Ann 改吃「片語」比對(`zelle payment to ann` / `to ann conf#`),保留防呆,不要退回裸 `contains "ann"`。

---

## 候選新規則(規則接不住的高頻 pattern)
> 是否編進知識庫**由 Jeff 決定,不自動改**。安全度 = 自動判錯的風險。

| Pattern | 筆數 | Jeff 的答案 | 安全度 | 備註 |
|---------|------|-------------|--------|------|
| `Zelle payment to Ann …` | 24 | cogs_tour | 高 | 即 Bug 2,等於補一條 vendor |
| 卡費自動扣款 `THANK YOU` / `AUTOMATIC PAYMENT` | 21 | transfer | 高 | 還卡費=搬自己的錢 |
| `WELLS FARGO CARD …CCPYMT`(operating 付 WF 卡) | 3 | cogs_tour | 中 | 付 WF 卡=代客機票成本;與上一條「還卡費=transfer」衝突,WF 卡要特例 |
| 航空 vendor 進帳 `United Airlines` / `Jupiter` 負數 | 4 | refund | 中 | 已知 travel vendor 退錢進來 → refund |
| 軟體訂閱 `Xsolla/Suno/Manus Ai/Moises/Creem/INTUIT/Dummyflight` | 18 | expense_software | 中 | 可建 software vendor 清單 |
| 餐飲 `McDonald/Jack in the Box/7-Eleven/Starbucks…` | 83 | 多數 expense_office | 低 | 同店有時 expense_travel(台灣行程脈絡),難硬規則 |
| BofA 存款 `BKOFAMERICA MOBILE/ATM` 進帳 | 34 | income_booking | 低 | 存款也可能是注資/信託,不能一律當營收 |
| `Zelle payment from <客人>` 進帳 | 47 | 多數 income_booking | 低 | 名字雜、且有 expense_office/cogs_tour 反例 → **「未知進帳不猜」是對的**,留給 LLM+Jeff |

## 紅線(維持)
- 不自動 remap 歷史 override;不自動改 `accountingKnowledge.ts`。
- 未知對方進帳 → `other_review`,不猜成收入(實測 47 筆 Zelle 進帳異質,證明這條該守)。
- 規則改動一律 Jeff 確認 → 改完跑 Vitest → tsc → 再上。

---

## ✅ 復測結果 — 2026-05-29(Jeff 確認 3 條規則後)

Jeff 經 AskUserQuestion 拍板:(1) 業主規則改 **出帳才判 transfer、進帳放生**;
(2) Ann 改吃片語比對 → `cogs_tour`;(3) WF 卡扣款 → `cogs_tour`、航空 vendor 進帳 → `refund`、軟體訂閱 → `expense_software`。改完跑 Vitest(34 綠)+ tsc(0 error)+ 同一份只讀腳本復測 603 筆:

Jeff 看完復測後再拍板第 4 條:**信用卡自動扣款(THANK YOU / AUTOMATIC PAYMENT 出帳)→ transfer**(21/21 全一致,排在 vendor + WF 卡之後,順序保護 WF→cogs_tour 例外)。下表為全部 4 條編完的最終狀態:

| 指標 | 改前(05-28) | 改後(05-29,4 條規則) | 變化 |
|------|-------------|----------------------|------|
| 確定性命中(省 LLM) | 82(13.6%) | **170(28.2%)** | +88 筆,覆蓋率翻倍 |
| 確定性精度 | 64.6%(53/82) | **100.0%(170/170)** | 每條 fire 的規則都對 |
| 🔴 規則判錯 | 29 | **0** | 業主進帳營收 bug 全清 |
| 該動手錯誤合計 | 326 | **227** | −99 |
| 中信心提示方向錯 | 1 | 2 | 仍 conf65 不拍板,進 LLM/Jeff |

- Bug 1(業主進帳 29 筆被誤判 transfer)→ **歸零**。掛業主名的進帳現在放生給 LLM/Jeff,不再從營收抹掉。復測也印證:`from CHUNFU HSIEH` 進帳裡 Jeff 多半標 income_booking,偶爾 transfer — 異質,不該硬判。
- Bug 2(Ann 24 筆漏接)→ 片語比對 `zelle payment to ann` 全接住,抽樣 `Zelle payment to Ann for "Ch…"`、`Ann Conf#…` 都命中 `cogs_tour` conf90,且不誤中 `annual`/`Ann Arbor`。
- 軟體(Xsolla/Moises/Dummyflight…)、WF 卡扣款、航空 vendor 退款 → 全部 deterministic 命中且對。
- anti-guess 不變式仍守:未知進帳不猜收入、refund 只給白名單 vendor。

### 第 4 條規則 — 已編(信用卡自動扣款 → transfer)
| Pattern | 筆數 | Jeff 歷史答案 | 一致性 | 結果 |
|---------|------|--------------|--------|------|
| 卡費自動扣款 `THANK YOU` / `AUTOMATIC PAYMENT`(出帳) | **21** | transfer | **21/21 全一致** | ✅ 已編入 `CARD_PAYOFF_PATTERNS`(source `card_payoff`,conf90,出帳才套)。排在 vendor(rule 2)+ WF 卡(rule 3)之後,WF 卡還款仍先被攔成 `cogs_tour`,順序保護的例外有單元測試鎖住。復測 21/21 全轉 rule_correct(其中 1 筆原本無 agent 紀錄),actionable 桶內 THANK YOU/AUTOMATIC PAYMENT 歸零。 |

### 仍待 Jeff 決定(未編)
目前**無**待決規則。候選清單(`Zelle payment from <客人>`、BofA 存款進帳、餐飲店家)安全度皆為低 — 名字雜、同店脈絡不同,硬規則風險高,維持「未知進帳不猜、留給 LLM+Jeff」的紅線。要再擴規則時回到上面「候選新規則」表重新評估。
