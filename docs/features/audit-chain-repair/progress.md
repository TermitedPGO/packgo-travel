# progress

- 2026-07-19:開批。prod 唯讀查核證據與根因見 proposal.md;設計 design.md。凍結:未 commit 未 push,零 schema/migration/prod 手改。
- 2026-07-19:T1-T8 落地。tsc 0;auditChain.test.ts 16 綠+既有 systemAudit 5 綠(mock 鏈同步);全套 375 檔/5712 tests 0 failed;突變抽核親跑:canonical 不截秒→D1 紅、legacy 跳過刪除→D4 兩紅,還原全綠;check-no-mock OK;diff --check 乾淨。範圍:6 改 2 新,零 schema/migration/prod 手改。對抗自查工作流跑批中;過後寫去信交 Codex。
- 2026-07-19(二輪,對抗自查後補洞):P1a ensure 存在判準加 isNotNull(rowHash)(孤錨死鎖排除,重啟自癒);P1b written 改寫後 re-query 證實,查不到回 failed(index.ts 記 error+進漏斗);P1c verify 回傳加 epochCount,UI 顯鏈錨點 id+多錨紅色警示(i18n zh/en);P2a backfill-passport-encryption.ts 旁路寫入者同步截秒+isNotNull tip;P2b design.md 補 D4.5 威脅模型(DB 寫權攻擊者單筆洗白+配套警示、pre-epoch 刪列不可偵測、進位非截斷措辭更正)、withAuditLogTip 註解與行為對齊;測試升級為 store+MySqlDialect 真 predicate 求值(沿用 clientBoot 模式)。23+5 綠、i18n parity 綠;四組突變親跑紅→綠(canonical 不截秒/legacy 跳過刪除/ensure isNotNull/tip isNotNull);tsc 0;全套 375 檔 5719 tests 0 failed;check-no-mock OK。凍結:未 commit 未 push。
- 2026-07-19(三輪,依 Codex R5 五條固定返工單):R5-1 ensure 改「最後一次 epoch attempt」判準(valid→orphan→restart 反例閉合、written 不得拿舊錨充數、空字串 hash 與 verifier 同判準 JS truthy、重複呼叫冪等,四組 regression);R5-2 錨定移出 startup → LOCAL_SCRIPT_TOKEN 端點 /api/admin/audit-chain-epoch + safe-deploy 以 flyctl machines list --json 證實全機同 image 才呼叫(token/機器證明/驗證任一缺=DEPLOYED_UNVERIFIED),withAuditLogTip 未取鎖不進 critical section(三次重試後改插無鏈孤列,fail-visible);R5-3 grant-admin.mjs 改鏈式寫入(截秒+isNotNull tip+canonical 欄位序快照+retry,export+grant-admin.test.mjs 6 測)、backfill 補同 payload retry、全庫 writer allowlist 掃描 regression(三入口,新 writer 即紅)、update mock 吃真 .set() payload(拿掉 rowHash 突變 6 紅);R5-4 UI 綠燈限定 chainOkFromAnchor(錨前 N 列明示未驗證)+tooltip/legacyRows 措辭修正+AuditLogTab.test.ts 5 render 測、design.md 威脅模型收緊(DB 寫權可換錨/刪錨、epochCount=1 非法證、外存首錨憑證是唯一外部約束)、proposal 1A0b gate 改五條件;R5-5 evidence v2 腳本(epoch 語意,另檔不覆寫)。驗證:tsc 0;auditChain 30+systemAudit 5+AuditLogTab 5+i18n 2 綠;safe-deploy 47(+7 錨定契約)、grant-admin 6(node --test);全套 376 檔/5731 tests 0 failed;突變 5(.set 拿掉 rowHash)6 紅→綠親跑,突變 6(written 不查基準)以語意簡化消除(冗餘條件移除+註解證明);check-no-mock OK。範圍:10 改 4 新(untracked 逐檔:AuditLogTab.test.ts、grant-admin.test.mjs、auditChain.test.ts、docs/features/audit-chain-repair/ 目錄含四 md);另 comms 夾新增 evidence v2 腳本一檔。凍結:未 commit 未 push。
- 2026-07-19(四輪,依 Codex R6 四條):R6-1 ensure 改「本次確切 insertId」證實(systemAuditStrict 不吞錯收據+eq(id) 重查;競爭 regression:before/after 間孤錨被別 writer 補 hash+INSERT 失敗→failed,突變 7 親跑紅→綠);R6-2 machines 證明綁定本次 release image(releaseImage 取自 flyctl releases --json,任何舊/過渡機即擋;old-only/new+old-stopping/取不到 image 三紅測)+endpoint 嚴格判準(正整數 epochStartId、anchor.id===epochStartId、64-hex、anomalyCount=0;malformed 三紅測)+端點抽 auditChainEpochEndpoint.ts factory(auth 短路/形狀/500 四純測)+storefrontBootPlan 清單補列+與 index.ts 實際註冊全量對帳測試+withAuditLogTip 重試帶等待(良性併發序列化,不再立刻退化孤列)+真雙 writer NX 競爭測試(兩列鏈上、previousHash 各異);R6-3 grant-admin timezone Z+PDT→DB→UTC round-trip regression+DB advisory lock(拿不到不進 critical section)+寫後 Y 叉機械偵測(forked→exit 1)、backfill 改走主通道 systemAuditStrict(同 Redis 鎖域,不再是旁路)、allowlist 掃描升級(正規化+alias 追蹤+多副檔名遞迴走檔+七組合成突變自證);R6-4 UI 分流改 epochStartId!=null(有錨且 legacyRows=0 補 render 測)+proposal/design 矛盾清除。驗證:tsc 0;auditChain 33+endpoint 4+bootPlan 8+AuditLogTab 6+systemAudit 5+i18n 2 綠;safe-deploy 55、grant-admin 11(node --test);全套 377 檔/5740 tests 0 failed;突變 7(ensure 退回 R5 語意)2 紅→還原綠親跑;SQL 登記表行號同步;check-no-mock OK。範圍:12 tracked modified+7 untracked 檔(auditChainEpochEndpoint.ts/.test.ts、AuditLogTab.test.ts、grant-admin.test.mjs、auditChain.test.ts、docs 四件套目錄、evidence v2 於 comms 夾)。凍結:未 commit 未 push。
- 2026-07-19(五輪,依 Codex R7 四條)【R8/R9 更正:本行的 image show 解析後被 R8 裁定為 machines 同源循環、收斂重鏈後被裁定仍有交錯窗、掃描器 symbol-aware 宣稱過度 —— 各於 R9 以 registry 權威/共鎖域/多跳閉包取代,見下方 R9 行;本行保留為歷史】:R7-1 release/machine image 正規化到同一 identity(release 必 Status=complete;ImageRef 剝 @sha256 後綴 lowercase 比全名 ref;release 帶 digest 則 machine digest 另比;所有回傳機器一律 started,同 image 過渡機也擋;全機 digest 唯一 sha256:)+五組紅綠測(真形狀 GREEN/同 digest stopping/unknown state/舊 digest/release 非 complete);R7-2 grant-admin 收斂重鏈(前驅變更即重算自身 hash,Codex 交錯 regression:app 先插孤列→grant 鏈舊 tip→app 補 hash→grant 收斂到 H_APP 前驅,可重算驗證;鏡像交錯 forked:true fail-closed;穩定情況單次 UPDATE);R7-3 掃描器 AST/symbol-aware(TypeScript AST+繫結定點迭代;namespace/destructuring/transitive 合成突變自證;repo 根遞迴+六副檔名+cheap 預過濾;現況兩入口)+storefront 對帳改雙向 set equality(missing/extra 都紅);R7-4 design(所有機器 started/五次帶等待/兩入口)、tasks(startCron 句)、本段口徑同步。檔數口徑:13 tracked modified+6 untracked path entries(展開 docs 目錄=9 untracked files)。測試實跑口徑:標準指令親跑;Codex 唯讀沙箱下 focused/全套用其 fallback,如實各記。
- 2026-07-19(六輪=第 5 回合上限,依 Codex R8 四條)【R9 更正:本行的 image show 權威解析被 R9 裁定為 machines 同源、「不支援退 Redis-only」錯誤路徑被裁定 fail-open、latch 測試被裁定未真等待 —— 各於 R9/R10 以 registry v2 API/全路徑 fail-closed/deferred 真等待取代,見下方各行;本行保留為歷史】:R8-1 expected digest 必來自 exact release/deploy artifact(release 自帶 digest 或 flyctl image show 權威解析 tag→digest,要求唯一+64-hex+tag 相符;取不到即紅;machine 主判準改 immutable digest exact 相等 /^sha256:[0-9a-f]{64}$/,tag optional 支援 tagless machine object;六組新測:tag-only 正/誤、解析不到、malformed digest、tagless GREEN、mixed digest);R8-2 真共鎖:app writer 在 Redis NX 內以 db.transaction 釘連線取同名 MySQL GET_LOCK(3s 超時走無鏈孤列;不支援時 error 退 Redis-only 且該狀態 grant 必拒寫=不會單邊裸奔),grant 端硬要求;三組 app 端測(鎖序 GET→tip→update→RELEASE、超時孤列、不支援退回)+grant 端 deferred/latch 真非同步 regression(app 持鎖期間 grant 等待、釋放後才進場、最終鏈線性無 Y 叉、app 鏈上 grant hash);R8-3 掃描器補括號/as/element access/賦值 alias 解包+跨檔 re-export 兩段式(全庫先收 export 別名再掃;Codex 四反例+跨檔案例全數合成自證;誠實邊界:非 TypeChecker 級,依賴識別字文字出現的閉包論證,design.md 記載);R8-4 design 誠實化(共鎖語意/掃描邊界)+r8-manifest.md(R7 基準與 R8 後各檔 SHA-256,內容級差額可證)。測試實跑口徑:本機標準指令親跑全綠;Codex 唯讀沙箱歷輪 fallback 各如實記於其裁定。
- 2026-07-19(停輪):Codex R8 差額終驗 FAIL P0 0/P1 2/P2 5(五輪機械數字屬實但兩承重斷點未閉:release digest 解析與 machines 同源循環、鎖錯誤路徑 fail-open;另 latch 測試未真等待、跨檔 re-export 多跳、文件舊宣稱、manifest 基準非先存)。R9 四條已開但**迴圈達單輪 5 回合上限,停輪待 Jeff 裁定**。凍結面:R6-1 全部、endpoint+五綠判準、release 自帶 digest exact 驗證、machine state/64-hex/mixed 防線、happy-path 共鎖+鎖逾時孤列+grant 拒寫、四 AST 反例、兩 writer 現況、storefront 雙向、UI/i18n/backfill/UTC/registryEntries、15 個 R7 frozen path SHA、三 evidence SHA。R8 manifest SHA(Codex 側算):ee357cecf212f58375bab65e0fe41f6966872761b2db20ea89da421f30061f89,待 Jeff 於工作樹外固定。未 commit 未 push;1A0b 維持關閉。
- 2026-07-19(七輪=R9 第 1 回合,依 Codex R9 四條):R9-1 expected digest 改自獨立權威 —— Fly registry v2 API manifest HEAD(與 Machines 完全無關,exact repository:tag),共用純函式 parseRegistryDigestHeaders(必 200+64-hex;401/redirect/缺標頭 throw)+五組真形狀 fixture 測;上輪 sha256:111/222 同源循環反例閉合(權威 B/機器 A 擋,marker 斷言);token 只進子行程 env 不落指令字串。R9-2 鎖錯誤路徑 fail-closed:transaction 缺失/BEGIN/GET_LOCK 錯誤 → 拒絕鏈式寫改無鏈孤列(絕不裸鎖重跑);fn 錯誤原樣上拋且至多一次;COMMIT-after-success 保留結果;四組新測 + production systemAudit+grant 共用 deferred 鎖後端真等待測試(app:lock-attempt 在 grant:release 前、acquired 在後、waitersPeak>0、最終 verifyAuditChain 全綠)。R9-3 多跳 re-export 閉包:collectExportedAliases 接受 seeds+本檔綁定閉包(aliased-import 再導出/const 後導出/second-hop 三變體自證)+全庫別名定點迭代。R9-4 design 觸發流程改 R9 口徑、鎖錯誤語意改 fail-closed、grant 註解改共鎖域、本檔 R7 行加更正標註、manifest 補 registryWhitelist R7 基準(git HEAD 物件推導,機械可驗)。
- 2026-07-19(八輪=R9 迴圈第 2 回合,依 Codex R10 四條):R10-1 tag 保留原大小寫(parseRef 只正規化 registry/repository 與 digest;uppercase ULID production regression+大小寫不符=不同 tag 擋)、parser 只認 terminal HTTP block(proxy CONNECT 200+origin 401 擋/origin 200 取 terminal digest)+manifest media type 白名單(text/html 擋)+digest 恰一(衝突擋),四組新 fixture;R10-2 RELEASE_LOCK 必回 1,throw/0/NULL → KILL CONNECTION_ID() 隔離污染 session 不回 pool(KILL 失敗記 CRITICAL),app 三測(release 0→KILL/throw→KILL/正常→不 KILL)+grant 端驗回傳(專用連線 conn.end 自動釋鎖,如實記錄);withAuditLogTip 舊「三次」註解更正。R10-3 writer guard 改真 TypeChecker(Program+getAliasedSymbol 收斂+element access 型別屬性解析+檔內資料流補強 let 賦值;虛擬多檔對抗案:namespace re-export 成員/default barrel/element via barrel/多跳循環全抓,shadow 與 decoy 同名不誤報;全庫掃描=真 tsconfig Program drizzle writer+正規化 raw SQL,恰兩入口)。R10-4 r8-manifest.md 還原精確外錨 bytes(ee357cec 親驗 MATCH),R9 基準移 r9-addendum.md(自身另行外錨)+git-object 證明力聲明縮窄;本檔 R8 行加更正標註;auditLog/design 舊宣稱清理。
- 2026-07-19(九輪=R9 迴圈第 3 回合,依 Codex R11 四條):R11-1 Content-Type cardinality(恰一且屬 manifest allowlist;重複相同/衝突前後三組 fixture 全拒);R11-2 Program roots 改 tsconfig 完整 fileNames(無字面預過濾;alias-only consumer 必入 Program)+BindingElement 追蹤(namespace destructure/改名 destructure/alias-only 三案自證);R11-3 raw SQL 靜態閉包(剝 /* */ 與 -- 註解+schema-qualified 支援;五正例二負例,動態拼接如實列邊界);R11-4 補錄 R9 差額裁定原文(自 stdout 完整還原,補錄時點如實標注)+R10 裁定代錄、auditLog ensure jsdoc(endpoint-only/epochCount!==1 必紅)、TTL-Y 叉舊宣稱(DB 共鎖下不可能雙 hashed 同前驅)、舊精度用語 → 進位口徑、dirty-count 精確口徑(git status 14 tracked+6 untracked entries=11 untracked files,共 25 dirty)。全部凍結面未倒退。
- 2026-07-20(十輪=R9 迴圈第 4 回合,依 Codex R12 四條):R12-1 obs-fold framing(terminal block 含任何前導空白/tab 折行先拒,再做 cardinality;mixed-case 綠案+folded value/藏第二 Content-Type/藏第二 digest 三紅案);R12-2 production 與虛擬共用同一 selectRoots+buildGuardProgram(字面預過濾復活會使虛擬 alias-only 案紅;selectRoots 結構釘測試)+destructuring assignment(左側 ObjectLiteral 的 PropertyAssignment/Shorthand 經 RHS 型別屬性 alias 收斂)+string-prop BindingElement;R12-3 SQL 靜態閉包改 AST 求值(evalStaticString:literal/template/+concat/檔內 const 解析;值內剝 #/--/塊註解;INSERT modifiers+quoted-hyphen schema;R11 整檔剝註解誤傷 bug 以回歸釘死;const concat 由誤列動態更正為靜態可判;runtime 參數/env 如實列邊界);R12-4 auditLog ensure jsdoc 改 endpoint-only 最終口徑、legacyRows「截斷」→「丟失(四捨五入非截斷)」。外錨雙 MATCH 未動。
- 2026-07-20(十一輪=R9 迴圈第 5 回合=上限,依 Codex R13 四條):R13-1 raw framing(block 不 trim;尾端純 SP/純 HTAB/SP+HTAB 三紅測);R13-2 production roots 完整性機械證明(literal-free 檔全數在 Program,字面預過濾突變必紅)+shorthand assignment/computed BindingElement/local object alias 三案;R13-3 SQL 改 quote/comment-aware tokenizer(executable comment /*!*/ 展開、引號內 #/-- 非註解、精確 table token —— decoy `adminAuditLog-shadow` 與 adminAuditLogArchive 不誤報)+lexical 求值(單檔 mini-Program checker,visited-set 防循環無深度上限;同名遮蔽雙向案+12-hop const 鏈案)+execute/query 呼叫限定(logger/文件字串不誤報)+repo root 遞迴走檔(含 shared/ 與根層);R13-4 過時稱謂改 writer A/B、重複呼叫測名、錨列 reason 改 precision-loss (rounding)、測試檔頭/proposal 舊精度用語改「丟失毫秒(四捨五入)」、design guard 邊界改 R13 真實口徑。外錨雙 MATCH 未動。本迴圈打滿 5 回合:本輪裁定後停輪向 Jeff 報告。
- 2026-07-20(停輪):Codex R13 差額終驗 FAIL P0 0/P1 0/P2 5(首呼叫斷線,同回合重試取得完整裁定)。R13-1 registry raw framing 整條 PASS(八案獨立實測);殘留四個可重現漏口全部在 guard 靜態分析工具(TypeChecker roots 只取 tsconfig fileNames 不含 scripts/*.ts、tagged template 求值缺、/*!50000 版本號 metadata 未剝、visited-set 無 unwind 把同 const 重用誤判 cycle)+progress 殘詞三處。修復主體(canonical/鎖/共鎖/endpoint/release-registry-machines 綁定/五判準)凍結面全數維持未倒退。R14 四條已由 Codex 固定,**R9 迴圈打滿 5 回合停輪,待 Jeff 裁定是否開新輪**。未 commit 未 push;1A0b 維持關閉。
- 2026-07-21(停輪):Codex R18 差額終驗 FAIL P0 0/P1 0/P2 2。R18-2(uncached 單路徑+exact paths+同參照)/R18-3(四副檔名承重)/R18-4(SHA/凍結)全 PASS;新預過濾經 Codex 自行構造反例攻擊未果(「相對 evaluator grammar 無已知 false-negative」入凍結面)。殘留兩項均為純測試補強:三反例需同時過完整 union、escaped-callee \u 例外需 mutation-killer(另 differential memo 措辭)。R19 四條已固定未啟動;**第三輪打滿 5 回合(累計三輪十五回合)停輪,待 Jeff 裁定**。未 commit 未 push;1A0b 維持關閉。
- 2026-07-21:Jeff 明示放行 commit(「我commit 吧」;Codex R18 FAIL P2 2 之下的指揮裁量,殘留兩項 guard 測試補強如實記載)。commit 023e8130 落地 25 檔;僅 commit,未 push 未 deploy;R19 已固定未啟動;1A0b 維持關閉。
