# 交接 — 2026-06-13 起的下週工作單

> 寫於 2026-06-12 週結(Jeff weekly usage 用盡)。新 session 先讀這份,不用翻舊對話。
> 全部代碼已 commit + push(main = 7c61bc7),全套 vitest 2340 passed,tsc 0。

## 第一件事:部署 v698(三個 feature 排隊中,全部已測綠)

待上線內容:
1. bank-csv-merge 全部(m0-m4):CSV 上傳自動合併 Plaid 泛詞交易、描述覆蓋、
   重分類、帳本詳情顯示銀行原始行。文件:docs/features/bank-csv-merge/
2. email-auto-reply Stage A(m0-m4):影子模式部署即生效(總開關 off,對外行為
   與部署前完全一致,只多證據記錄)+ 系統頁政策卡 + 今日待辦自動回記錄 box。
   文件:docs/features/email-auto-reply/
3. 流程閉環修復(7c61bc7):核准寄出的回覆寫進客戶往來時間軸

部署流程(不變):Jeff 在 terminal 跑
`echo "packgo-$(date +%s)" > /Users/jeff/Desktop/網站/.deploy-approve`
→ session 跑 `DEPLOY_TOKEN="$(cat .deploy-approve)" pnpm ship`(七 gate,token 用完即焚)。

## 部署後驗收(Jeff 親驗,各 5 分鐘)

- CSV 合併:記帳頁上傳一份真 BofA 月度 CSV → 預覽顯示「將合併 N 筆」→ commit →
  帳本裡 PURCHASE 變完整描述、卡 other_review 的重新分類
- 影子模式:等幾封真客人信進來 → 今日待辦出現「AI 自動回覆記錄」box 的影子卡;
  系統頁政策卡的成績單開始有數字
- 流程閉環:回一封信(審核卡核准或編輯並回覆)→ 該客人頁的來信往來出現「我方回覆」

## 兩週後(約 6/26)

- 看系統頁成績單:某類別達標(20 封 + 95% 不改核准率,拍板)→ Jeff 自行決定
  是否在政策卡開該類真自動(🔒 確認)。session 不主動催。

## Backlog(不急,Jeff 點哪個做哪個)

- 聊天窗化:客戶頁內直接回信(對話串視圖 + 頁內 composer,寄信重用既有 🔒 路)
  — 2026-06-12 流程討論時 Jeff 知悉,未拍板
- 海報 price-in-image guard(UAT B-03,P2)
- 毛利匯率換算(Lion 成本 TWD vs 售價 USD,現顯示「幣別不同」;有現成 exchangeRate router)
- 行程庫效能:2,635 筆只載 1,000 + 首載 8-10 秒(server 分頁/virtual scroll)
- 淨利 alert 雙桶重複顯示(P3)、EN 模式 AI 分類標籤(P3)、cost gate dialog a11y(Info)
- 批3 mockup 缺項:匯出稽核 §17550 鈕、「全部接受 AI 建議」鈕
- AdminV2 archive(/admin-v2-archive)穩定後移除
- Plaid 描述診斷存檔:docs/features/admin-chat-claude-parity/backlog-plaid-descriptions.md
  (CSV 分類方向 bug 一併記在裡面)

## 本週收檔摘要(2026-06-09 ~ 06-12)

- 後台重設計 8 批全部完成 + 深度 UAT + 修復 → v690-v697 → THE FLIP(/admin → /workspace)
- 批9 email 迴路:escalation 編輯並回覆、email 歸戶、訪客進 sidebar + 記錄頁
- bank-csv-merge + email-auto-reply 兩個 feature 開發完(待部署)
- 鐵律狀態:碰錢碰法律(退款/投訴/報價/訂金/簽證)永不自動,寫死在代碼;
  自動回覆走信任階梯,Stage B 開關只能 Jeff 在 UI 親手開
