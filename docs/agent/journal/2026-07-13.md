# 2026-07-13(B1 批;實際執行於 07-12 深夜起的接手 session,Jeff 指示一個一個來)

## 做了什麼
- B1 信託認列 fail-closed:PLAN-2026-07-13 第一優先,完成到「已合併 main」(commit 77045fc,未部署)。
  - recognizeReadyDepartures 移除,改 scanRecognitionDue 純掃描零寫入;唯一寫入點(原 trustDeferralService.ts:716)不存在了。
  - worker/trustRecognizeNow 只產 agentMessages 待審卡(同集合去重、Redis 讀失敗照出卡、歸零清記憶)+每日 notifyOwner 待審摘要(卡去重但摘要天天發,錢的可見性不沉默)。
  - 守門測試三層掃產線碼防復活(字面/raw SQL 賦值/.set 區塊),紅綠自證過;旗標四組合零寫入。
  - 對抗審查抓到的兩條低風險同批收:守門 regex 三層化、test-phase4-e2e.mjs raw UPDATE recognizedAt 後門封除。
  - client 認列確認閘拆除改唯讀掃描,i18n parity 綠。

## 驗收紀律執行
- 指揮親跑:守門 grep 0 命中、tsc 0 錯、trust 8 檔 121 綠、i18n parity 3 檔 551 綠;錢路徑 diff 逐行親讀。
- 三層驗證:fresh opus 對抗審查 PASS(第 1 層)+ 確定性不變量測試 13 條(第 2 層);第 3 層=部署後次日 cron 觀察零自動認列,pending 等 Jeff ship。
- 審查回饋分類:守門 regex 拼字級屬 prompt 可防(派工單只要求 grep 字面,未要求語意級),已回流認知;e2e 腳本後門屬不可預知(盤點時只掃 .ts,.mjs 腳本漏網),教訓=影響面盤點必含 scripts/*.mjs。

## Jeff 須知
- 認列現為全庫零寫入:逐筆核准端點等 CPA 矩陣核准後另開批,建成前認列全停擺(裁定的 fail-closed 態)。
- 要生效需 pnpm ship(只有你)。ship 後次日 cron 我會驗零自動認列收第 3 層。

## 明日方向
- B2 結帳模式一硬封(B1 驗收閉環後的下一個高風險批)。
- 桌面 Codex 通信檔已補 B1 運行證據待傳段。
