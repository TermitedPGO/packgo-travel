# 執行計劃(Codex 第6輪裁定後,2026-07-13 起)

> 原則:文件寫「已修」不算,控制真正生效才算(Codex 6輪:控制生效度僅 3.5/10)。每項附完成線=可重現證據,非派工單。WIP:同時最多 1 高風險批。

## 第一優先:兩條活的動錢/資料路徑做成真 fail-closed(程式停止線,非再寫聲明)

### B1 信託認列 worker fail-closed(最急,每日在跑)
- 現況(已核實):trustRecognitionQueue 每日跑,把到期訂金自動寫 recognizedAt(trustDeferralService.ts)。PLAID flag ON=活的。三筆目前 unmatched 未被動,但下一筆對上的客款會被自動認列=違反「認列是 Jeff 動錢權」。
- 改法:不關 flag(關會退回立即認列更糟)。保持 deferred,移除所有自動 recognizedAt 寫入,worker 只產 Jeff 待審卡;等 CPA 認列矩陣+律師提領矩陣核准後 Jeff 逐筆按。
- 完成線:測試釘死「worker 跑完 recognizedAt 仍 NULL、只產卡」;prod 部署後觀察一輪零自動認列。

### B2 結帳模式一硬封
- 現況(已核實):featureFlags + bookingsPayment 仍以旗標 ON 進模式一即時 capture;runbook-flag-enable 仍教開旗標。
- 改法:保留驗價驗位元件供模式二重用,移除/硬封模式一 capture 入口與開旗標 runbook;加測試:無論 legacy flag 如何設,模式二(授權+供應商確認+capture_before+Jeff核准)鏈未建前不得建立立即收款 Session。
- 完成線:mode1-cannot-revive 測試綠。

## 第二優先:DB 硬化重設計(收回「一小時做掉」)

### C1 migrator 憑證真隔離
- 現況(已核實):db-role-hardening 把 MIGRATION_DATABASE_URL 設同一 packgo-travel app secret=長跑 runtime 讀得到,只隔離變數名沒隔離權限。
- 改法:migrator 用獨立 one-shot app/機器/外部部署工作,正式長跑 app 永遠看不到 DDL 憑證;migrator 身分短效/用後撤銷輪替;缺專用 migrator credential 時 migration 非零退出不 fallback root;release_command 跑完立即跑 schema contract 不符即非零退出;Fly readiness 改 /readyz 含 DB 契約,紅燈不得回 0。
- 完成線:證明正式 runtime 看不到 DDL credential + canary 四類 SQLSTATE 全拒 + 實際還原 RPO/RTO。這是權限切換+復原演練,非貼 SQL。

## 第三優先:證據與文件收斂

### D1 證據包儲存邊界(部分已做)
- 已做:移至 ~/legal_hold 非同步、雜湊零損、700/600、iCloud 原件保留。
- 待:加密位置(Jeff 建加密磁碟映像/外接碟)、律師決定 iCloud 副本處置、搬前後 hash manifest。

### D2 全路由舊規則收斂(Codex P1.1 清單)
- 待改 active 檔:PROJECT-BRIEF.md:53、admin-chat-claude-parity/design.md:92、windows-resident-setup.md:24、public-site/rebuild-plan.md:208、backend.md:31-35(email/phone 自動重用)、workflow.md:5-16(30行門檻)、20-judgment.md J2/J5/J7、backend.md:87(progress 寫已部署)。
- 交付:allowlist + 剩餘命中清單,非只說搜過。

### D3 STATE 重寫
- 現況違反自己規則(DB硬化標完成實為分支未合、等 Jeff 七項超 WIP 3、分歧歸零單方宣布)。
- 改法:只留 2 在飛主題(合規補救、DB/財務停止線),其餘進 backlog;完成詞逐項可重現 evidence_reference;修 grants evidence 指向(scratchpad 不在 main)。

### D4 A3 表撤回出發日二分 + 措辭修正
- A3 的「未出發補回/已出發認列」是撤回的舊規則換表重現。改:提領看 §17550.15(c) 具體用途+證據,認列是另一 CPA 問題,出發日非共同開關。$12,424 標「條件式情境估算」非法定補回額。§17550.14/17550.19 措辭照 Codex 精確版。無安全港:補回=provisional remediation 非已治癒。

## 只有 Jeff
三筆出發日+客人、等額資金隔離(非補入,律師定)、加密證據位置、律師+CPA、正本下載。

## 凍結(附 exit criteria,P0 沒關不解凍)
通道 adapter、雄獅、UV 全量、省稅顧問、後台佔位頁。exit=P0 全關+商業試驗首評過。

## 商業試驗(P0 全關後)測 assisted funnel(非站上直接購買,即時結帳 OFF)
陌生訪客→有效詢問→核價驗位→正式報價→接受→人工受控收訂金。首評:30天≥100有效詢問,記流量源/各轉換/Jeff分鐘/SLA/救援率/錯價錯位/退款/貢獻毛利。任何錯價錯位錯付款承諾即停。
